(wildcard_cert) {
    tls {
        dns cloudflare
        wildcard
    }
}

#https://caddy.yourdomain.com {
#  proxy  / {%CADDY_DOCKER_GATEWAY%}:{%CADDY_NODE_PORT%}
#  basicauth / root {%CADDY_NODE_ROOT_PWD%}
#}